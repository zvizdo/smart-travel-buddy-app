GOOGLE_CLOUD_PROJECT=
GOOGLE_MAPS_API_KEY=
API_KEY_HMAC_SECRET=

# Multi-user HTTP mode: URL of the deployed MCP server (Cloud Run)
MCP_SERVER_URL=https://your-cloudrun-url

# Stdio mode only (local dev): single-user API key resolved at startup
# MCP_API_KEY=stb_...
